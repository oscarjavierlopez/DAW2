generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Users {
  id     Int    @id @default(autoincrement())
  nombre String
  email  String @unique
  edad   Int
}